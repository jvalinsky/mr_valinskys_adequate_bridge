// Package smoke runs deterministic integration tests for the bridge pipeline,
// exercising firehose ingestion, record mapping, SSB publishing, and the
// admin UI in a self-contained local environment.
package smoke
