{
  "permissions": {
    "allow": [
      "Bash(deciduous nodes --branch main | head -50)",
      "Bash(wc:*)",
      "Bash(SPLIT_DIR=/tmp/history-splits\nmkdir -p \"$SPLIT_DIR\"\n\n# Split Claude history into even chunks\nLINES=$(wc -l < ~/.claude/history.jsonl)\nCHUNK_SIZE=$(( LINES / 3 + 1 ))\nsplit -l $CHUNK_SIZE ~/.claude/history.jsonl \"$SPLIT_DIR/claude-\"\n\n# Split Codex history\nLINES=$(wc -l < ~/.codex/history.jsonl)\nCHUNK_SIZE=$(( LINES / 4 + 1 ))\nsplit -l $CHUNK_SIZE ~/.codex/history.jsonl \"$SPLIT_DIR/codex-\"\n\n# Rename to .jsonl\nfor f in \"$SPLIT_DIR\"/*; do mv \"$f\" \"$f.jsonl\" 2>/dev/null; done\n\necho \"Split files:\"\nls -la \"$SPLIT_DIR\")",
      "Bash(go version 2>&1 || echo \"Go not found\")",
      "Bash(cat:*)",
      "Bash(deciduous nodes --branch main | tail -50)",
      "Bash(git merge:*)",
      "Bash(cd /Users/jack/Software/mr_valinskys_adequate_bridge && GOFLAGS=-mod=mod go test ./internal/bridge -run FollowerTracker -count=1)",
      "Bash(npx tsx:*)",
      "Bash(cp /Users/jack/.codex/skills/adequate-bridge-web-ui-builder-critique/references/*.md /opt/homebrew/lib/node_modules/@letta-ai/letta-code/skills/adequate-bridge-web-ui-builder-critique/references/)",
      "Bash(cd /opt/homebrew/lib/node_modules/@letta-ai/letta-code/skills && node --eval \"\nconst fs = require('fs');\nconst path = require('path');\nconst skillDir = 'adequate-bridge-web-ui-builder-critique';\nconst files = [];\n\nfunction walk(dir) {\n  for (const f of fs.readdirSync(dir)) {\n    const full = path.join(dir, f);\n    if (fs.statSync(full).isDirectory()) walk(full);\n    else files.push(full);\n  }\n}\nwalk(skillDir);\n\nconsole.log('Skill files:');\nfiles.forEach(f => console.log('  ' + f));\nconsole.log('\\\\nSkill ready at: ' + path.resolve(skillDir));\n\")",
      "Bash(rg:*)",
      "Bash(GOFLAGS=-mod=mod go test ./pkg/atproto/syntax/... -v 2>&1 | head -40)"
    ]
  }
}